{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "desciption": "${mod_description}",
  "authors": [
    "Rashing"
  ],
  "license": "GPL-3.0",
  "environment": "${mod_environment}",
  "entrypoints": {
    "client": [
      "org.example.multiversion_mod.ExampleMod"
    ]
  },
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": "${minecraft_version}",
    "java": ">=${java_version}",
    "fabric-api": ">=${fabric_api_version}"
  }
}
